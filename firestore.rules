rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // صرف تصدیق شدہ صارفین کو تمام دستاویزات پڑھنے کی اجازت دیں۔
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
